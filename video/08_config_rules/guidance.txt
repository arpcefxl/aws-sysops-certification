# Assumes that there is an existing AWS Config stream already configured

# Create rule for access-keys-rotated as an example of a scheduled automated task

# (Optional) configure Remediation Action of AWS-PublishSNSNotification
